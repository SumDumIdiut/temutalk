node_modules/
.env
.cert-*.pem
.cert-ip.txt
devices.json
hubs.json
bin/win/node.exe
bin/win/cloudflared.exe
bin/linux/node
bin/linux/cloudflared
bin/linux/piper/
voices/
.cloudflared/config.yml
.cloudflared/*.json
.cloudflared/cert.pem

# Generated by Setup.sh — machine-specific
darkice.cfg

# install.sh runtime state
.run/
logs/

# Logs
server.log
*.log

# Python
__pycache__/
*.pyc
telemetry/

# Orphaned now that lib/chat.js is removed, but the file itself may still
# exist on disk (old runtime data) -- keep ignored so it never gets
# accidentally committed.
.chat-state.json
